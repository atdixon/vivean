-- Nullable NotNull etc applied to VMap methods and other values (default_ are Nullable, e.g.)
    ... use javax.annotation and shade? or copy to V?

-- java 8 date and time and locale etc
-- sql date and time
-- collection and array coercions?
-- package-info.java explaining core philosophy
        (* knowledge-orientation; so coerce if 'reasonable interpretation'
         * throws fast FastCannotCoerceException
         * conversion to collection types??___
         * KEY: null -> null OR null -> emptyCollection, depending
                ...->collection gets 'many' behavior
                ...->non-colloction gets 'one' behavior
         * ...)

-- get rid of insist() for perf -- or use assert???


-- instead of insist, use java8.Objects utils


--EXPLAIN:
    -- Kilt handles item containers, which includes List, Optional, etc. but NOT Map



-- show off (mapping) usecases like:

    site.one("webpres", String.class, result::add); // to add to list
                ... // to add to map